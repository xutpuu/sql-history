{
  "name": "sql-history",
  "displayName": "SQL History",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.86.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": ["onStartupFinished"],
  "main": "./extension.js",
  "contributes": {
    "commands": [{
      "command": "sql-history.saveHistory",
      "title": "SQL History: Save History"
    },
    {
      "command": "sql-history.saveAllTabsHistory",
      "title": "SQL History: Save All History"
    },
    {
      "command": "sql-history.logOpenDocuments",
      "title": "SQL History: Open Documents"
    },
    {
      "command": "sql-history.saveAllTabsHistory",
      "title": "SQL History: Save All History"
    }]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.86.0",
    "@types/mocha": "^10.0.6",
    "@types/node": "18.x",
    "eslint": "^8.56.0",
    "typescript": "^5.3.3",
    "@vscode/test-cli": "^0.0.4",
    "@vscode/test-electron": "^2.3.9"
  }
}
